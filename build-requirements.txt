# Build dependencies for CryptoUniverse Enterprise
# These must be installed BEFORE main requirements to prevent setuptools.build_meta errors

# Core build tools
pip>=23.0.0
setuptools>=65.0.0
wheel>=0.40.0

# Additional build dependencies that may be needed
packaging>=21.0
pyparsing>=3.0.0
